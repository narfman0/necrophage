[package]
name = "psychophage"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"

# Enable optimizations for dependencies in debug builds
[profile.dev.package."*"]
opt-level = 3

[profile.dev]
opt-level = 1
